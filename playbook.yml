---
- hosts: all
  sudo: yes
  remote_user: root
  vars:
  - aaa2: "{{aaa|default('CC')}}"
  - bbb2: "{{bbb|default('BB')}}"
  - ccc2: "{{ccc|default('AA')}}"
  tasks:
  - name: PrintSomething
    debug: var=aaa2
  - name: PrintSomething
    debug: var=bbb2
  - name: PrintSomething
    debug: var=ccc2
